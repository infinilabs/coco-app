import React from 'react'
import ReactDOM from 'react-dom/client'
import ListDesktop from './list_desktop.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ListDesktop />
  </React.StrictMode>,
)
